import getCodeSubmissions from './getCodeSubmissions.js';

const problemId = 'ITP1_2_C';
const language = 'Python3';
const submissionSize = 500;

getCodeSubmissions(problemId, language, submissionSize);
